# Apify API Credentials
# Get your API token from https://console.apify.com/account#/integrations
#APIFY_USER_ID=your_user_id_here
APIFY_PERSONAL_API_TOKEN=your_api_token_here

# PostgreSQL Database Credentials
# Update these to match your local PostgreSQL setup
PGHOST=localhost
PGDATABASE=volfefe_machine_dev
PGUSER=postgres
PGPASSWORD=your_postgres_password_here

# VPN Proxy Configuration (for Polymarket API access)
# Polymarket geo-blocks US IPs. Enable VPN proxy to access CLOB/Gamma APIs.
#
# Setup:
#   1. Get WireGuard key from https://account.proton.me → VPN → WireGuard
#   2. Run: docker compose -f docker-compose.vpn.yml up -d
#   3. Set VPN_PROXY_ENABLED=true
#
PROTONVPN_WIREGUARD_PRIVATE_KEY=your_wireguard_private_key_here
VPN_PROXY_ENABLED=false
VPN_PROXY_HOST=localhost
VPN_PROXY_PORT=8888
VPN_SERVER_COUNTRY=Netherlands
